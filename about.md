# 🎸 Smart Adaptive Guitar Tab Generator - Project Architecture

เอกสารนี้รวบรวมคำอธิบายโครงสร้างและหน้าที่ของแต่ละไฟล์ในโปรเจกต์ เพื่ออธิบายการทำงานภาพรวมของแอปพลิเคชัน

## 📂 Root Directory (รากของโปรเจกต์)
- **`api_main.py`**
  **หน้าที่:** ไฟล์หลักตัวขับเคลื่อนเซิร์ฟเวอร์ (FastAPI) ทำหน้าที่รอรับพัสดุ (Requests) จากหน้าเว็บและกระจายงานไปให้โมดูลอื่นทำงานต่อ 
  ประกอบด้วย API Endpoints หลัก:
  - `POST /api/tab/generate`: รับโน้ตไปสร้างแทป
  - `POST /api/tab/override`: รับการแก้ไขโน้ตรายตัวและสั่งตีเส้นทางใหม่
  - `GET /api/learning/stats`: ส่งข้อมูล AI กลับให้หน้าจอ Dashboard
  - คอยเสิร์ฟไฟล์ HTML เวลาพิมพ์เรียกจากเว็บไซต์เบราว์เซอร์

- **`README.md` & `idea.md`**
  **หน้าที่:** เอกสารอธิบายภาพรวมโปรเจกต์ (Readme มาตรฐาน) และสมุดจดไอเดียเริ่มต้นการออกแบบระบบ

- **`requirements.txt`**
  **หน้าที่:** รายการชื่อไลบรารีของ Python (เช่น fastapi, uvicorn) เอาไว้ให้รันคำสั่ง `pip install -r requirements.txt` โหลดมารันติดเครื่องได้เลย

## 📂 core/ (สมองกลและฐานข้อมูล)
- **`core/guitar_logic.py`**
  **หน้าที่:** "หัวใจหลักของอัลกอริทึมกีตาร์" ทำหน้าคิดคำนวณคณิตศาสตร์เพื่อแปลงจากข้อมูลดิบเป็นตำแหน่งลายนิ้ว 
  - แปลงชื่อตัวโน้ตเป็นค่า MIDI (`note_to_midi`)
  - ค้นหาพิกัดทั้งหมดบนสายกีตาร์ที่มีเสียงนั้นๆ (`find_all_positions`)
  - คัดกรองและประมวลผลทิศทางเสียงเช่น ข้ามอ็อกเทฟ `^` (สูงขึ้น) และ `%` (ต่ำลง)
  - ฟังก์ชันหาแนวทางนิ้วที่สั้นและขยับน้อยที่สุด (Greedy Closest Path) พร้อมระบบหักล้างระยะทางนิ้ว (Bonus Distance Weights)

- **`core/models.py`**
  **หน้าที่:** "นักสืบตรวจสอบข้อมูล" (Pydantic Models) คอยจับผิดและบังคับให้ข้อมูล JSON ที่ผู้ใช้ส่งมาจากหน้าเว็บ (เช่น ชื่อโน้ต, ขอบเขต Fret, สายที่อนุญาต) มีประเภทและค่าที่ตีกรอบไว้ ไม่ให้เกิดบั๊กล่มกลางคันก่อนนำไปประมวลผล

- **`core/learning.py`**
  **หน้าที่:** "หน่วยความจำ AI" เก็บประวัติ (EditHistory) เวลาที่ผู้ใช้กดแก้แทปเองที่หน้าจอ (Manual Override) ตัวแปรนี้จะจดจำพิกัดโปรดของคุณเอาไว้ แล้วนำไปแจก **คะแนนโบนัส** ให้อัลกอริทึมใน `guitar_logic.py` โอนเอียงมาเลือกตำแหน่งที่คุณชื่นชอบในครั้งต่อไปเสมอ

## 📂 static/ (ระบบหน้าบ้าน)
- **`static/index.html`**
  **หน้าที่:** สิ่งเดียวที่ผู้ใช้มองเห็น (Frontend) เขียนด้วยเทคโนโลยีคลาสสิค Vanilla HTML, CSS และ Javascript โดยไม่ต้องพึ่ง Framework หนักเครื่อง หน้าที่หลักคือ:
  - อินเทอร์เฟซรับตัวอักษรโน้ตและเงื่อนไขสายกับ Fret Box
  - วาดภาพ ASCII Tablature เรียงสายกีตาร์ให้สวยงาม
  - พื้นที่ Interactive ที่สามารถคลิกที่โน้ตรายตัวในกล่องด้านล่างและส่งค่ากลับไปแก้ยัง Server โดยตรง
  - ระบบ Download Tab เป็นไฟล์นามสกุล `.txt` และแผงหน้าปัด AI Dashboard Memory

## 📂 tests/ (ระบบตรวจสอบคุณภาพ)
- **`tests/test_guitar_logic.py`**
  **หน้าที่:** ด่านตรวจคุณภาพโค้ดอัตโนมัติ (Automated Unit Tests) หากใครแอบเขียนแก้สมการระยะโน้ตหรือทำฟังก์ชันดนตรีพัง ไฟล์นี้จะส่งสัญญาณเตือนคุณทันทีเวลาที่เรารันคำสั่งเทส เพื่อป้องกันไม่ให้โครงสร้างตรรกะระดับลึกเน่าหรือเพี้ยนก่อนนำไปใช้งานจริง
